
https://www.geeksforgeeks.org/problems/check-whether-k-th-bit-is-set-or-not-1587115620/1?itm_source=geeksforgeeks&itm_medium=article&itm_campaign=bottom_sticky_on_articleclass Solution
{
    public:
    // Function to check if Kth bit is set or not.
    bool checkKthBit(int n, int k)
    {
        // or we can do by shifting the element k times right and then checking with & 1
        // or by simply modulo and divide
        //  Constant time
        if( (n & (1<<k) )>0){
            return true;
        }
        else return false;
    }
